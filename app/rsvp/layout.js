export const metadata = {
    title: "RSVP",
    description:
        "RSVP for Basir & Noor's wedding. Submit your details and guest information.",
};

export default function RsvpLayout({ children }) {
    return children;
}
